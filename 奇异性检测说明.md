# 机器人奇异性检测 - 雅可比矩阵计算说明

## 概述
这段代码用于计算机器人末端执行器的雅可比矩阵，以便进行奇异性检测。雅可比矩阵描述了关节速度与末端执行器速度之间的关系。

## 数学背景

### 1. 雅可比矩阵的定义

雅可比矩阵 **J** 建立了关节空间速度与笛卡尔空间速度之间的关系：

\[
\dot{\mathbf{x}} = \mathbf{J}(\mathbf{q}) \dot{\mathbf{q}}
\]

其中：
- \(\dot{\mathbf{x}} \in \mathbb{R}^6\)：末端执行器的速度（3个位置速度 + 3个旋转速度）
- \(\dot{\mathbf{q}} \in \mathbb{R}^n\)：关节速度向量（n为关节数）
- \(\mathbf{J}(\mathbf{q}) \in \mathbb{R}^{6 \times n}\)：雅可比矩阵，依赖于当前关节角度 \(\mathbf{q}\)

### 2. 雅可比矩阵的分解

雅可比矩阵可以分解为位置部分和旋转部分：

\[
\mathbf{J} = \begin{bmatrix}
\mathbf{J}_p \\
\mathbf{J}_r
\end{bmatrix}
\]

其中：
- \(\mathbf{J}_p \in \mathbb{R}^{3 \times n}\)：位置雅可比矩阵（描述位置速度）
- \(\mathbf{J}_r \in \mathbb{R}^{3 \times n}\)：旋转雅可比矩阵（描述角速度）

## 代码步骤详解

### 步骤 1: 初始化雅可比矩阵

```python
jacp = np.zeros((3, self._mj_model.nv))  # 位置雅可比
jacr = np.zeros((3, self._mj_model.nv))  # 旋转雅可比
```

**数学含义：**
- \(\mathbf{J}_p \in \mathbb{R}^{3 \times n_v}\)：位置雅可比，初始化为零矩阵
- \(\mathbf{J}_r \in \mathbb{R}^{3 \times n_v}\)：旋转雅可比，初始化为零矩阵
- \(n_v\)：MuJoCo 模型中的速度自由度数量（`self._mj_model.nv`）

**为什么是 3×nv？**
- 3：表示三维空间（x, y, z）
- nv：表示所有可能的自由度（包括关节、自由体等）

### 步骤 2: 选择末端执行器

```python
ee_site_name = 'wrist_3_link'
```

**物理含义：**
- 选择 `wrist_3_link` 作为末端执行器的参考点
- 这是 UR5e 机器人的最后一个腕部关节，代表机器人末端
- 在这个点计算雅可比矩阵，可以得到末端执行器的速度映射关系

### 步骤 3: 计算雅可比矩阵

```python
mujoco.mj_jac(self._mj_model, self._mj_data, jacp, jacr, 
              self._mj_data.body(ee_site_name).xpos, 
              self._mj_model.body(ee_site_name).id)
```

**数学公式：**

MuJoCo 的 `mj_jac` 函数计算：

\[
\begin{bmatrix}
\mathbf{J}_p \\
\mathbf{J}_r
\end{bmatrix}
= \frac{\partial}{\partial \mathbf{q}} \begin{bmatrix}
\mathbf{p}_{ee}(\mathbf{q}) \\
\boldsymbol{\omega}_{ee}(\mathbf{q})
\end{bmatrix}
\]

其中：
- \(\mathbf{p}_{ee}(\mathbf{q}) \in \mathbb{R}^3\)：末端执行器位置（从 `xpos` 获取）
- \(\boldsymbol{\omega}_{ee}(\mathbf{q}) \in \mathbb{R}^3\)：末端执行器角速度

**函数参数说明：**
- `jacp`：输出参数，存储位置雅可比 \(\mathbf{J}_p\)
- `jacr`：输出参数，存储旋转雅可比 \(\mathbf{J}_r\)
- `xpos`：计算点的位置（末端执行器位置）
- `body_id`：计算点的物体ID

**计算原理：**

对于位置雅可比，每个元素计算为：

\[
J_{p,ij} = \frac{\partial p_{ee,i}}{\partial q_j}
\]

对于旋转雅可比，每个元素计算为：

\[
J_{r,ij} = \frac{\partial \omega_{ee,i}}{\partial \dot{q}_j}
\]

### 步骤 4: 提取相关关节的雅可比

```python
J = jacp[:3, :6]  # 取位置部分的前 6 关节
```

**数学含义：**

\[
\mathbf{J} = \mathbf{J}_p[0:3, 0:6] \in \mathbb{R}^{3 \times 6}
\]

**为什么只取前6个关节？**
- UR5e 机器人有 6 个主要关节：
  1. shoulder_pan_joint（肩部旋转）
  2. shoulder_lift_joint（肩部升降）
  3. elbow_joint（肘部）
  4. wrist_1_joint（腕部1）
  5. wrist_2_joint（腕部2）
  6. wrist_3_joint（腕部3）
- 这6个关节控制末端执行器的位置
- 其他自由度（如夹爪、自由体等）不影响位置控制

**矩阵结构：**

\[
\mathbf{J} = \begin{bmatrix}
\frac{\partial x}{\partial q_1} & \frac{\partial x}{\partial q_2} & \cdots & \frac{\partial x}{\partial q_6} \\
\frac{\partial y}{\partial q_1} & \frac{\partial y}{\partial q_2} & \cdots & \frac{\partial y}{\partial q_6} \\
\frac{\partial z}{\partial q_1} & \frac{\partial z}{\partial q_2} & \cdots & \frac{\partial z}{\partial q_6}
\end{bmatrix}
\]

## 奇异性检测的应用

### 奇异性条件

当雅可比矩阵的行列式为零或接近零时，机器人处于奇异位置：

\[
\det(\mathbf{J} \mathbf{J}^T) = 0 \quad \text{或} \quad \det(\mathbf{J} \mathbf{J}^T) \approx 0
\]

**物理含义：**
- 在某些关节配置下，机器人失去了某些方向的运动能力
- 即使关节速度很大，末端执行器在某些方向上也无法移动
- 这会导致控制不稳定或无法实现某些目标位置

### 条件数（Condition Number）

另一种检测方法是使用条件数：

\[
\kappa(\mathbf{J}) = \frac{\sigma_{\max}}{\sigma_{\min}}
\]

其中 \(\sigma_{\max}\) 和 \(\sigma_{\min}\) 是 \(\mathbf{J}\) 的最大和最小奇异值。

- \(\kappa \to \infty\)：接近奇异
- \(\kappa \approx 1\)：远离奇异，控制良好

## 完整的速度映射关系

最终得到的速度映射关系为：

\[
\begin{bmatrix}
\dot{x} \\
\dot{y} \\
\dot{z}
\end{bmatrix}
= \mathbf{J} \begin{bmatrix}
\dot{q}_1 \\
\dot{q}_2 \\
\dot{q}_3 \\
\dot{q}_4 \\
\dot{q}_5 \\
\dot{q}_6
\end{bmatrix}
\]

这表示：给定6个关节的速度，可以计算出末端执行器在x、y、z方向上的速度。

## 总结

1. **初始化**：创建3×nv的零矩阵用于存储位置和旋转雅可比
2. **选择参考点**：确定末端执行器的位置（wrist_3_link）
3. **计算雅可比**：使用MuJoCo函数计算完整的雅可比矩阵
4. **提取子矩阵**：只保留位置部分的前6个关节，得到3×6的雅可比矩阵

这个雅可比矩阵可以用于：
- 奇异性检测
- 逆运动学求解
- 速度控制
- 力控制


